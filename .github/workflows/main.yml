# Workflow to sync changes from source repo        
name: Sync repo
on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
      security-events: write

    # Sync from Origin repo
    steps:
    - name: Checkout Target Repo
      uses: actions/checkout@v2
      with:
        ref: main
        
    - name: Git Pull Action
      run: git pull
      
    - name: Check if TargetFolder exists
      id: check_target_folder
      run: |
        if [ -d "TargetFolder" ]; then
          echo "::set-output name=exists::true"
        else
          echo "::set-output name=exists::false"
        fi
      
    - name: Add user config
      run: |
        git config --global user.email "csd0108@163.com"
        git config --global user.name "Abel-csd"  

    - name: Add Subtree
      if: steps.check_target_folder.outputs.exists != 'true'
      run:
        git subtree add --prefix=TargetFolder https://username:${{ secrets.PAT }}@github.com/Abel-csd/OriginRepro.git main
    
    - name: git fetch
      run: 
        git fetch https://username:${{ secrets.PAT }}@github.com/Abel-csd/OriginRepro.git main
    
    - name: Pull Subtree
      run: |
        git subtree pull --prefix=TargetFolder https://username:${{ secrets.PAT }}@github.com/Abel-csd/OriginRepro.git main

    - name: Git Push Action
      run: git push
          
